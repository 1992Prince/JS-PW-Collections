🌐 JSON Basics
👉 JSON = JavaScript Object Notation
 👉 Mainly data store aur exchange karne ke liye use hota hai (API response, config files, test data etc.).

1. JSON Array – list of values
Hamesha square brackets [ ] me hota hai.


Ye ek ordered list hota hai.


Example:
[
  "Apple",
  "Mango",
  "Banana"
]

👉 Yeh ek JSON Array of strings hai.
 👉 Isme sirf values hai (Apple, Mango, Banana).

2. JSON Object
Hamesha curly braces { } me hota hai.


Ye ek map / dictionary format hota hai (key → value pair).


Example:
{
  "name": "Rahul",
  "age": 25,
  "isDeveloper": true
}

👉 Ye ek JSON Object hai.
Keys = name, age, isDeveloper


Values = "Rahul", 25, true



3. Array of Objects
Ye ek array hai jisme multiple JSON objects hain.


Bahut common hota hai APIs me.


Example:
[
  {
    "name": "Rahul",
    "age": 25
  },
  {
    "name": "Neha",
    "age": 28
  }
]

👉 Ye ek JSON Array hai
 👉 Is array ke andar 2 JSON objects hain.

4. Nested JSON Object (Object inside Object)
Ek object ke andar doosra object ho sakta hai.


Example:
{
  "user": {
    "name": "Rahul",
    "address": {
      "city": "Delhi",
      "pincode": 110001
    }
  }
}

👉 Breakdown:
Root object = { }


user ek key hai jiska value ek aur object hai


Uske andar address ek key hai jiska value bhi ek object hai



5. Complex Example – Mix of All
{
  "company": "TechCorp",
  "employees": [
    {
      "id": 1,
      "name": "Rahul",
      "skills": ["Java", "Playwright", "SQL"],
      "address": {
        "city": "Delhi",
        "zip": 110001
      }
    },
    {
      "id": 2,
      "name": "Neha",
      "skills": ["Python", "AWS"],
      "address": {
        "city": "Mumbai",
        "zip": 400001
      }
    }
  ]
}

👉 Breakdown:
Root object {} hai.


Uske andar ek key "company" aur ek key "employees" hai.


"employees" ek array hai.


Array ke andar objects hain (each employee).


Har employee ke object me ek aur nested object "address" hai.


"skills" ek array of strings hai.

🔥 Interview-Friendly Differences

Term
Syntax
Example
Use Case
JSON Array
[ ]
["a","b","c"]
Simple list of values
JSON Object
{ }
{"name":"Rahul","age":25}
Key-value pairs
Array of Objects
[ { } ]
[{"id":1},{"id":2}]
List of objects (like employees)
Nested JSON Object
{ { } }
{"user":{"address":{"city":"Delhi"}}}
Hierarchical data
Mix (Complex JSON)
{ [ { } ] }
Company with employees + nested address
Real APIs


👉 Soch, API se data aata hai:
Agar ek list of tags aayi → JSON Array


Agar ek single user info aayi → JSON Object


Agar ek list of users aayi → Array of Objects


Agar har user ke andar address bhi ho → Nested JSON Object

1) If we have JSON Array, can we give it key name?
👉 JSON Array [ ] khud se key nahi le sakta, but JSON Object ke andar array ho to us array ko key ke through naam diya ja sakta hai.
Example:
{
  "fruits": ["Apple", "Mango", "Banana"]
}

Yaha "fruits" ek key hai.


Uska value ek JSON Array hai.


🚫 Agar sirf array likha:
["Apple", "Mango", "Banana"]

→ Ye valid JSON hai but keyless hai (direct array).
Conclusion:
Array directly → bina key ke.


Object ke andar → key ke naam se store ho sakta hai.



2) What datatypes are allowed for JSON object key and values?
✅ Keys:
Sirf string type allowed hai.


Aur woh double quotes me hona zaroori hai.
 ❌ Keys number ya boolean ya array nahi ho sakte.


{ "name": "Rahul" } ✅
{ name: "Rahul" } ❌ (JS me chalega, JSON me nahi)

✅ Values:
String → "Rahul"


Number → 25


Boolean → true/false


Null → null


Object → { "city": "Delhi" }


Array → ["Apple","Mango"]



3) In JSON Object, can we have multiple JSON Objects like Array has?
👉 JSON Object { } ke andar multiple keys ho sakti hain, aur har ek key ke value me ek aur JSON Object ho sakta hai.
 But ye map type structure hota hai, na ki array jaisa.
Example:
{
  "user1": { "name": "Rahul", "age": 25 },
  "user2": { "name": "Neha", "age": 28 }
}

Yaha ek object hai jisme 2 nested objects hain (user1 aur user2).
🚫 Lekin isme order guarantee nahi hoti (JSON Object unordered hota hai).
 Agar tumhe ordered collection of multiple objects chahiye → use Array of Objects.

4) If we are defining testdata.json file for data-driven testing → Array or Object?
👉 Ye use case pe depend karta hai.
Case 1 – Agar ek hi object hai with fixed keys
{
  "username": "testuser",
  "password": "password123"
}

✅ Object best hai, kyunki ek hi set of data hai.

Case 2 – Agar multiple test cases ke liye alag-alag data chahiye
[
  { "username": "user1", "password": "pass1" },
  { "username": "user2", "password": "pass2" }
]

✅ Array of Objects best hai (kyunki tum loop chala ke data-driven testing kar sakte ho).

🔥 Best Practices for JSON in Testing
Readable aur formatted JSON use karo → indentation 2 ya 4 spaces.


Meaningful key names rakho → username, password, email (not a1, b2).


Consistency maintain karo → ekhi level pe data ka structure same ho.


Example: Agar ek employee object me id hai, to dusre me bhi id hona chahiye.


Avoid comments in JSON → JSON officially comments support nahi karta.


For multiple test sets → Array of Objects use karo


For single config/testdata → Simple Object use karo


Always keep JSON separate from code (e.g., resources/testdata.json).



👉 Interview-ready line:
"For single set of static data, I prefer JSON Object. But for multiple records in data-driven testing, Array of Objects is better as I can loop through them easily in my test runner."
