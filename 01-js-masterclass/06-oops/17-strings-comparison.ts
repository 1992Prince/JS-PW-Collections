// ✅ 1. Using === (Strict Equality) — Recommended


let str6 = "hello";
let str7 = "hello";

console.log(str6 === str7); // true ✅

// === checks value and type — safest and most accurate.
// It does not care about reference — only the actual text matters.