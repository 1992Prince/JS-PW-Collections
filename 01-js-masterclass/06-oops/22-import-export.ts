/**
Why Use import and export?
Instead of writing all your code in one big file, you can split it into small files (modules) 
and import what you need — for example, utility functions, classes, or constants.

 */

import { add, PI } from './23-mathUtils';

console.log(add(5, 3));     // Output: 8
console.log(PI);            // Output: 3.14

console.log();


